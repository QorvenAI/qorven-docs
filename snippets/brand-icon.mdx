export const BrandIcon = ({ src, name, size = 32 }) => (
  <img
    src={src}
    alt={`${name} logo`}
    width={size}
    height={size}
    style={{ display: 'inline-block', verticalAlign: 'middle', margin: '0 6px 0 0' }}
  />
);
