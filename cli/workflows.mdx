---
title: "qorven workflows"
description: "Manage workflows"
sidebarTitle: "workflows"
icon: "git-branch"
---

<Info>Generated from `qorven workflows --help`. Re-run `scripts/gen-cli-reference.sh` after CLI changes.</Info>

## Reference

```text
Manage workflows

Usage:
  qorven workflows [command]

Available Commands:
  delete      Delete a workflow
  get         Get workflow details
  list        List workflows
  toggle      Enable or disable a workflow

Flags:
  -h, --help   help for workflows

Global Flags:
  -o, --output string   Output format: table, json, yaml (default "table")
      --server string   Gateway URL (env: QORVEN_SERVER)
      --token string    Auth token (env: QORVEN_TOKEN)
  -v, --verbose         Verbose output
  -y, --yes             Skip confirmation prompts

Use "qorven workflows [command] --help" for more information about a command.
```

## Examples

<Note>Example section to fill with real operator usage patterns. TBD.</Note>

## Related

<CardGroup cols={2}>
  <Card title="CLI index" icon="terminal" href="/cli/index">All commands.</Card>
  <Card title="Global flags" icon="flag" href="/cli/global-flags">`--server`, `--token`, `--output`.</Card>
</CardGroup>
