---
title: "qorven mcp"
description: "Manage MCP servers"
sidebarTitle: "mcp"
icon: "plug"
---

<Info>Generated from `qorven mcp --help`. Re-run `scripts/gen-cli-reference.sh` after CLI changes.</Info>

## Reference

```text
Manage MCP servers

Usage:
  qorven mcp [command]

Available Commands:
  create      Register a new MCP server
  delete      Delete MCP server
  get         Get MCP server details
  list        List MCP servers
  test        Test MCP server connection
  tools       List tools from MCP server

Flags:
  -h, --help   help for mcp

Global Flags:
  -o, --output string   Output format: table, json, yaml (default "table")
      --server string   Gateway URL (env: QORVEN_SERVER)
      --token string    Auth token (env: QORVEN_TOKEN)
  -v, --verbose         Verbose output
  -y, --yes             Skip confirmation prompts

Use "qorven mcp [command] --help" for more information about a command.
```

## Examples

<Note>Example section to fill with real operator usage patterns. TBD.</Note>

## Related

<CardGroup cols={2}>
  <Card title="CLI index" icon="terminal" href="/cli/index">All commands.</Card>
  <Card title="Global flags" icon="flag" href="/cli/global-flags">`--server`, `--token`, `--output`.</Card>
</CardGroup>
